# API base URL when the React UI is deployed separately from the backend.
# Omit or leave empty when using Vite dev server (proxy) or when UI is on the same host as the API.
# Examples:
#   Local API:  http://localhost:8080
#   Production: https://api.yourdomain.com
VITE_API_URL=http://localhost:8080
